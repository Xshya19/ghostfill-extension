# Repository metadata draft

Apply these values in the GitHub repository settings after reviewing the release package. This document does not change remote repository settings.

**Description**

Chrome extension for disposable email, password generation, and deterministic OTP extraction during signup verification.

**Topics**

`chrome-extension`, `manifest-v3`, `disposable-email`, `temporary-email`, `otp-autofill`, `password-generator`, `form-autofill`, `typescript`, `react`, `privacy`

**Social preview**

Use `docs/assets/ghostfill-social-preview.png` as the 1280 by 640 social-preview image. Review it before uploading in GitHub repository settings. The existing SVG workflow diagram remains the README preview.
