/**
 * ╔══════════════════════════════════════════════════════════════════╗
 * ║  DATASET GENERATOR — Bridge to PyTorch Training               ║
 * ║  Uses JSDOM to simulate browser environment for extraction.    ║
 * ╚══════════════════════════════════════════════════════════════════╝
 *
 * BROKEN: This script references FormSimulatorV2 which was removed from
 * the repo. A replacement simulator class is needed before this can run.
 */

console.error(
  'ERROR: FormSimulatorV2 does not exist in the repo. ' +
    'This script needs a rewrite before it can generate training data.'
);
process.exit(1);
